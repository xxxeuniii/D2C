"""
反幻觉相关 Prompt

来源: 原 anti_hallucination.py
"""

# ============================================
# 第二层防御：Prompt 约束强化（嵌入代码生成 Prompt）
# ============================================

ANTI_HALLUCINATION_PROMPT = """
## 防幻觉规则（必须严格遵守）:
1. 只使用以下文档中出现的组件 API，不要编造属性名
2. 如果不确定某个属性是否存在，不要使用它
3. 组件的标签名必须与文档一致（如 <el-button> 不能写成 <el-btn>）
4. 属性的拼写必须与文档一致（如 type 不能写成 theme）
5. 事件绑定格式：Vue 用 @click，React 用 onClick
6. 如果文档中没有某个用法，用注释标注 "// TODO: 确认 API"
"""

# ============================================
# 第四层防御：LLM 交叉验证 Prompt 模板
# ============================================

CROSS_VALIDATION_PROMPT = """
## 代码验证任务

请对照以下组件文档，逐行检查代码中使用的 API 是否正确。

## 组件文档（正确答案）:
{component_docs}

## AST 静态分析发现的问题:
{ast_issues}

## 需要检查的代码:
```{lang}
{code}
```

## 检查规则:
1. 每个组件的标签名必须与文档一致
2. 每个属性的拼写必须与文档一致
3. 如果代码中出现了文档没有的属性，标记为幻觉
4. 如果代码中出现了文档没有的事件，标记为幻觉

## 输出格式:
如果代码完全正确，输出 "PASSED: 所有 API 与文档一致"
如果有问题，输出:
1. 幻觉列表（哪个组件、哪个属性/事件是编造的）
2. 修复后的代码（修正所有幻觉 API）
"""
